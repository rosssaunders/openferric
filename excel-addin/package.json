{
  "name": "openferric-excel-addin",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.11.0"
  },
  "scripts": {
    "clean": "rm -f functions.js functions.js.map taskpane.js taskpane.js.map serve.js serve.js.map",
    "build": "npm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "npm run build && node serve.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.5",
    "@types/office-js": "^1.0.421"
  }
}
